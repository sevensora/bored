#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int x;
    do
    {
        x = get_int ("Please enter height: ");
    }
    while (height < 1 || height > 8);

    for
    {
        print("\n");
    }
}