#include <cs50.h>
#include <stdio.h>
#include < string.h>


int main(void)
{
    int letters = 0;
    int sentences = 0;
    int words = 0;
    string = get_string("Text: ");

    for (int i = 0; i < strlen(text); i++)
    {
        if(text[i] > 65 )
    }
}